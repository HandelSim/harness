[project]
name = "harness-claude-agent"
version = "0.1.0"
description = "Harbor adapter for harness claude"
requires-python = ">=3.10"
dependencies = ["harbor"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["harness_claude_agent"]
