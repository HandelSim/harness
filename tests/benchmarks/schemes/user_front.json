{
  "name": "user_front",
  "description": "Production prompt-injection scheme as captured at this repo's HEAD: full cooperative-tool-use scaffolding on the last user message, request placed BEFORE the tool list. Maps to PROXY_PROMPT_MODE=user_front (the proxy.py default). The baseline most A/B runs compare against.",
  "env": {
    "HARNESS_PROXY_SCHEME": "user_front",
    "PROXY_PROMPT_MODE": "user_front"
  }
}
