schema_version = "1.2"
artifacts = []

[task]
name = "harness/hello-harness"
description = "Trivial wiring-test task: create /app/hello.txt. Multi-arch (ubuntu:24.04) so it runs natively on aarch64 without binfmt."
authors = ["harness"]
keywords = ["smoketest", "wiring"]

[metadata]

[verifier]
timeout_sec = 60.0

[verifier.env]

[agent]
timeout_sec = 300.0

[environment]
build_timeout_sec = 300.0
os = "linux"
cpus = 1
memory_mb = 1024
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
